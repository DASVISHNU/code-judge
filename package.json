{
  "name": "code-judge",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "turbo": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}